mbc-externalApplications-services
=================================

A consumer for external applications to send messages to. The payload and routing key settings will trigger various functionality within the Message Broker system.
